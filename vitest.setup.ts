// Root vitest setup — herdada por packages/api/vitest.setup.ts (que faz `import "../../vitest.setup"`).
// Mantém setup vazio por enquanto. Adicionar aqui mocks globais ou env vars de teste se precisar.
export {}
